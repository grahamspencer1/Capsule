<section class="single-entry-container current-entry">
  <% if current_user == @entry.user %>
    <p>
      <%= link_to edit_entry_path(@entry), class: "edit-btn" do %>
        <i class="far fa-edit fa-xs"></i>
        <span>Edit</span>
      <% end %>|

      <%= link_to entry_path(@entry), class: "delete-btn", method: :delete, data: {:confirm => "Are you sure you want to delete this entry?"} do %>
          <i class="far fa-trash-alt fa-xs delete-btn"></i>
        <span>Delete</span>
      <% end %>
    </p>
  <% end %>

  <div class="entry-title-wrapper">
    <h1><%= @entry.title %></h1>
    <h3><%= @entry.created_at.strftime("%b/%d/%Y") %></h3>
  </div>

  <p class="entry-content"><%= @entry.content %></p>
</section>

<div class="bg2" style="background: url('<%= image_path(@entry.bg_picture.image) %>')"></div>
