<h1>This is the single show page </h1>

<div class="single_entry_container">
  <h1><%= @entry.title %> </h1>
  <h1><%= @entry.content %> </h1>
  <h1><%= @entry.created_at.strftime("%b %d %Y")%> </h1>

  <button class="reward" onclick="goEntry(-1)">&#10094;</button>
  <button class="forward" onclick="goEntry(1)">&#10095;</button>

</div>

<!-- <script>

  var entryIndex = 1;
  showEntry(entryIndex);

  function goEntry(n) {
    showEntry(entryIndex += n);
  }
  function showEntry(n) {
      var i;
      var x = document.selectqueery("entry");
      if (n > x.length) {entryIndex = 1}
      if (n < 1) {entryIndex = x.length} ;
      for (i = 0; i < x.length; i++) {
          x[i].style.display = "none";
      }
      x[entryIndex-1].style.display = "block";
  }
</script> -->
